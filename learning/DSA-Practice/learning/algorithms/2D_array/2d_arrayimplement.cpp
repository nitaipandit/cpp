#include <iostream>
int main() {

  int i, j, r, c;
  std::cout << "enter the no of row :";
  std::cin >> r;
  std::cout << "enter the no of coll :";
  std::cin >> c;
  int mat[r][c];

  for (i = 0; i < r; i++) {
    for (j = 0; j < c; j++) {
      std::cout << "enter the element:";
      std::cin >> mat[i][j];
    }
  }
  for (i = 0; i < r; i++) {
    for (j = 0; j < c; j++) {
      std::cout << mat[i][j];
    }
    std::cout << std::endl;
  }
}
int x;
std::cout << "enter the search index:";
std::cin >> x;
for (i = 0; i < r; i++) {
  for (j = 0; j < c; j++) {
    if (x == mat[i][j]) {
      std::cout<:class
      public QObject {
        Q_OBJECT;

      public:
        explicit(QObject *parent = nullptr);

      signals:

      public slots:
      };
      <j
    }
  }
}
